<h1>Voici une magnifique liste de types de films</h1>

<ul>
  <% @lists.each do |list| %>
  <li><%= link_to list.name, list_path(list.id) %></li>
  <% end %>
</ul>

<%= link_to "Create new list", new_list_path %>
